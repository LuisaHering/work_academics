@model IEnumerable<SN_WebMVC.Models.LaboratoryViewModel>

@{
    ViewBag.Title = "laboratórios";
    Html.RenderPartial("_SideBar");
}

<h2>Meus laboratórios</h2>

<p>
    @Html.ActionLink("Encontrar laboratório", "Search")
</p>

<p>
    @Html.ActionLink("Novo laboratório", "Create")
</p>

<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Descricao)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Descricao)
        </td>
        <td>
            | @Html.ActionLink("Entrar", "Details", new { id=item.Id }) |
        </td>
    </tr>
}

</table>
